{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "libtilebox unit tests",
            "program": "${workspaceFolder}/build/tilebox/tests/tilebox_tests",
            "args": [],
            "cwd": "${workspaceFolder}"
        },
    ]
}
